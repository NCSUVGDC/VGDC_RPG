/**
 * simply used to determine if stones/banners are currently assigned to other stones/banners
 * when reenabling buttons - so we dont reenable buttons for already assigned banners/stones
 * 
 * @author Andrew Karcher
 */

using UnityEngine;
using System.Collections;

public class isAssignedBool : MonoBehaviour {

    public bool isAssigned = false;
}
